# 开源版开发约定

用户明确要求优先。先检查工作树，保留已有修改；先读相关实现和测试，再做最小必要改动。

- `apps/web` 仅通过 `packages/contracts` 与 CMS 协作，不导入 Payload、CMS 或数据库驱动。
- `apps/cms` 拥有认证、权限、业务状态和数据访问；跨集合变更进入显式用例与事务。
- 用户写入显式传递 `req` 和操作上下文，使用 `overrideAccess: false`；系统绕权须局部且有明确理由。
- `packages/contracts` 只放 DTO、运行时 Schema 与稳定标识，不放持久化文档、用例或 UI。
- 保持 `push: false`。已应用迁移不可修改；新增结构、回填、生成类型和验证作为一组审查。
- 已发布表单快照不可修改；历史答案按原版本解释。考虑重复请求、并发和失败回滚。
- 日志和提交不得含密钥、密码或真实个人信息；测试只使用专用的本机虚构数据库。
- UI 不能代替服务端授权。页面处理加载、空、成功、校验失败、拒绝与不可用状态，保持键盘可用。
- 不新增推测性 Repository、事件总线、DSL 或重复权限规则。
- 影响安装、配置、账号、发布、备份或排错时同步更新 `运维手册（人读）`；避免复制多份规则。
- 验证相关行为并运行 `pnpm check`；按风险补构建、迁移或端到端测试，不能宣称未执行的验证通过。
- v1.0.0 是首个测试版，发布必须标记 prerelease；不得描述为经过长期生产验证。

开发入口：[docs/development/README.md](docs/development/README.md)。
